## Бот погоды и новостей по городам России (500 тыс.+)

**Единственная папка с ботом:** `tg bot2\russian-weather-tg-bot`. Старые копии удалены — запускайте только отсюда (файл **ЗАПУСТИТЬ_БОТА.bat**).

**Важно:** если токен от BotFather был выдан для **@Russianweather1_bot** (с цифрой 1), то в Telegram нужно открывать именно его: **https://t.me/Russianweather1_bot**  
Бот **@Russianweather_bot** (без единицы) — это другой бот, ему нужен свой токен.

**Возможности:**
- **Погода** — текущая погода по любому городу из списка 500 тыс.+ (Open-Meteo, без ключа).
- **Новости** — актуальные новости по городу (RSS, Дзен, при наличии ключа NewsAPI).
- **Города** — города РФ с населением от 200 тыс. (включая Донецк, Луганск, Севастополь и др.). В меню — топ‑10 крупнейших; остальные через **🔍 Поиск города**.
- **Мини-приложение «Сайт погоды»** — многостраничный Web App: список городов, страница погоды по каждому городу (как Яндекс/Google), карта России с отметкой города, фото исторического центра (если загружены в `weather_app/assets/historic_{slug}.png`).
- **Мини-игра Pac-Man** — мини-приложение (Telegram Web App): лабиринт, точки, призраки, очки и жизни. Управление: стрелки или кнопки на экране.
- **Напоминание о погоде** — подписка на ежедневную рассылку по городу: в выбранное время (по Москве) приходит прогноз на день (утро, день, вечер, ночь). Можно указать своё время (ЧЧ:ММ) или выбрать из списка. Подписки хранятся в `data/weather_reminders.json`.

Для работы нужен только **TELEGRAM_TOKEN** в `.env`. Ключи погоды и новостей не обязательны.

---

### 1. Окружение

```powershell
cd "c:\Users\Administrator\Desktop\tg bot2\russian-weather-tg-bot"
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env`:

```text
TELEGRAM_TOKEN=ваш_токен_от_BotFather
```

Опционально:

- **NEWS_API_KEY** — новости по городу через NewsAPI.
- **VK_ACCESS_TOKEN** — подтягивать посты новостных групп ВКонтакте (РИА, Лента, РБК и др.). Токен: [vk.com/dev/access_token](https://dev.vk.com/docs/access_token) (права: доступ к стене).
- **CAPTCHA_2CAPTCHA_API_KEY** — для обхода капчи Дзена при загрузке новостей из dzen.ru (платный сервис [2captcha.com](https://2captcha.com)). Без ключа при капче показываются новости из RSS.
- **MINI_APP_URL** — HTTPS-адрес мини-игры Pac-Man (папка `mini_app/`). Без него кнопка «🎮 Pac-Man» покажет инструкцию по настройке.
- **WEATHER_APP_URL** — HTTPS-адрес сайта погоды (папка `weather_app/`). Без него кнопка «🌐 Погода (приложение)» и команда `/app` покажут инструкцию по настройке.

**Новости из Дзена:** бот загружает статьи с dzen.ru через браузер (Playwright). После `pip install -r requirements.txt` выполните `playwright install chromium`. Чтобы **реже получать капчу бесплатно**, в скрипте включены «стелс»-настройки браузера и случайные задержки; надёжного бесплатного решения для Yandex SmartCaptcha нет — при появлении капчи можно задать ключ 2Captcha или пользоваться новостями из RSS. Проверка: `python dzen_scraper.py "Челябинск" 5`.

Новости из **Telegram** (каналы СМИ) подмешиваются автоматически через RSS-мосты (RSSHub, tg.i-c-a.su), ключи не нужны.

### 3. Запуск

**Рекомендуется:** дважды щёлкнуть по **ЗАПУСТИТЬ_БОТА.bat** в этой папке (запуск всегда из правильной папки).

Или вручную:
```powershell
cd "c:\Users\Administrator\Desktop\tg bot2\russian-weather-tg-bot"
python bot.py
```

Откройте бота в Telegram (например, `@Russianweather1_bot`) и отправьте `/start`.

### 4. Команды и меню

- `/start` — карта России, приветствие и блок меню (кнопки внизу экрана).
- `/menu` — открыть меню: Справка, Выбор города, Погода, Новости, Старт и карта, Меню.
- `/city` — выбор города (погода и новости).
- `/weather` — только погода по городу.
- `/news` — только новости по городу.
- `/game` — мини-игра Pac-Man (если задан `MINI_APP_URL`). В том же сообщении — кнопка «Сайт погоды», если задан `WEATHER_APP_URL`.
- `/app` — открыть мини-приложение погоды по городам (кнопка «Открыть приложение»). Работает, если в `.env` задан `WEATHER_APP_URL`.
- `/remind` — подписка на ежедневную рассылку погоды (выбор города и времени по Москве).
- `/help` — справка.

### 5. Мини-приложение Pac-Man

Игра лежит в папке **`mini_app/`** (один файл `index.html`). Чтобы кнопка «🎮 Pac-Man» и команда `/game` открывали игру в Telegram:

1. **Разместите `mini_app` по HTTPS** — Telegram открывает только HTTPS-ссылки. Варианты:
   - **GitHub Pages**: репозиторий → Settings → Pages → Source: main, папка `/mini_app` или корень (положите `index.html` в корень и укажите `https://username.github.io/repo/`).
   - **Vercel / Netlify**: загрузите папку проекта, укажите корень или `mini_app` как корень статики.
   - **Свой сервер**: отдавайте папку `mini_app` через nginx/apache по HTTPS.
2. В **`.env`** добавьте (URL должен заканчиваться на `/`, если у вас именно страница папки):
   ```text
   MINI_APP_URL=https://ваш-домен.com/mini_app/
   ```
   Или, если `index.html` в корне сайта: `MINI_APP_URL=https://ваш-домен.com/`.
3. Перезапустите бота. В меню появится кнопка «🎮 Pac-Man»; по нажатию или по команде `/game` бот пришлёт кнопку «▶ Играть Pac-Man», открывающую игру во встроенном браузере Telegram.

### 6. Сайт погоды (Web App / Mini App)

В папке **`weather_app/`** лежит многостраничное приложение: список городов (62 города, в т.ч. Донецк, Луганск, Севастополь), по клику — страница города с текущей погодой, прогнозом на 2 дня (макс/мин), картой России с отметкой города и блоком для фото исторического центра. Приложение адаптировано для **Telegram Mini App**: полноэкранный режим (`expand`), подстройка под тему (светлая/тёмная), отступы под безопасную зону (вырез, индикатор).

**Быстрый способ (GitHub Pages):** в проекте есть workflow **`.github/workflows/deploy-weather-app.yml`**. Залейте проект в репозиторий GitHub, в настройках репо включите **Pages** → **Source: GitHub Actions**, сделайте push в `main` — сайт появится по адресу `https://<USER>.github.io/<REPO>/`. В `.env` уже добавлен пример: `WEATHER_APP_URL=https://gpro7724-netizen.github.io/russian-weather-tg-bot/` (подставьте свой репо). Подробно: **КАК_ВКЛЮЧИТЬ_САЙТ_ПОГОДЫ.md**.

Альтернатива:
1. **Разместите `weather_app` по HTTPS** (GitHub Pages вручную, Vercel, Netlify или свой сервер). URL должен указывать на папку, где лежат `index.html`, `app.js`, `cities.json` и при необходимости `assets/`.
2. В **`.env`** добавьте:
   ```text
   WEATHER_APP_URL=https://ваш-домен.com/weather_app/
   ```
3. Перезапустите бота. В меню появится кнопка **«🌐 Погода (приложение)»** (внизу экрана и в блоке inline-кнопок). По нажатию откроется мини-приложение в Telegram. Также доступны команда **`/app`** и кнопка «🌡 Сайт погоды» в сообщении по команде `/game`.

**Фото городов:** чтобы на странице города показывалось фото исторического центра, положите в `weather_app/assets/` файлы `historic_{slug}.png` (например `historic_moscow.png`). Можно скопировать из папки `assets/` бота, если они там уже есть.

**Обновление списка городов:** после изменения городов в `bot.py` выполните `python export_cities_json.py` — будет пересобран `weather_app/cities.json`.

Картинки городов (в боте): до 5 фото исторического центра (красота города, без промышленных) + до 3 достопримечательностей. Имена файлов: `historic_{slug}.png`, `historic_{slug}_2.png` … `historic_{slug}_5.png` в папке `assets/`.  
**Правило:** только исторический центр города и достопримечательности; на фото не должно быть промышленности (заводы, трубы, склады).  
Скрипты: `download_historic_photos.py` (главное фото по городу), `fetch_historic_center_photos.py` (по 3 фото по поиску Commons), `download_city_photos.py` (по списку файлов Commons). Для новых городов в скриптах заданы запросы/файлы только по центру и достопримечательностям.

# Как показать в боте мини-приложение «Сайт погоды» (многостраничник)

В боте уже прописан адрес сайта в `.env`:
```text
WEATHER_APP_URL=https://gpro7724-netizen.github.io/russian-weather-tg-bot/
```

Чтобы по этому адресу открывался ваш многостраничник:

1. **Создайте репозиторий на GitHub** (если ещё нет), например `russian-weather-tg-bot`.  
   Склонируйте или залейте проект так, чтобы в репо были папки `weather_app`, `.github/workflows` и остальные файлы.

2. **Включите GitHub Pages** в настройках репозитория:  
   **Settings** → **Pages** → **Build and deployment** → **Source**: выберите **GitHub Actions**.

3. **Сделайте push в ветку `main` (или `master`)**.  
   Workflow «Deploy Weather App to GitHub Pages» загрузит папку `weather_app` на GitHub Pages.

4. Подождите 1–2 минуты. Сайт будет доступен по адресу:
   ```text
   https://gpro7724-netizen.github.io/russian-weather-tg-bot/
   ```

5. **Перезапустите бота** (если он уже был запущен).  
   В меню появятся кнопки **«🌐 Погода (приложение)»** и **«🌡 Сайт погоды»**, команда `/app` будет открывать мини-приложение.

---

Если репозиторий называется иначе (не `russian-weather-tg-bot`), измените в `.env` строку:
```text
WEATHER_APP_URL=https://gpro7724-netizen.github.io/ВАШЕ_ИМЯ_РЕПО/
```

После первого деплоя кнопка в боте будет открывать ваш многостраничник по погоде.
